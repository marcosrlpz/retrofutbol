const mongoose=require("mongoose");const bcrypt=require("bcryptjs");require("dotenv").config();mongoose.connect(process.env.MONGO_URI).then(async()=>{const User=require("./src/models/user.model");const hash=await bcrypt.hash("Retro2026!",10);const r=await User.findOneAndUpdate({email:"maria1@email.com"},{email:"pedidos.retrofutbol@gmail.com",password:hash});console.log(r?"Listo":"No encontrado");process.exit();});
